apiVersion: v1
kind: Service
metadata:
  name: {{ include "freeby-jenkins.fullname" . }}
  labels:
    app: {{ include "freeby-jenkins.name" . }}
    chart: {{ include "freeby-jenkins.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "freeby-jenkins.name" . }}
    release: {{ .Release.Name }}
